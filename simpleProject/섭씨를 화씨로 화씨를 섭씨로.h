#pragma once
#define _CRT_SECURE_NO_WARNINGS
#include <stdio.h>



/*
float CelToFah(float Cel)
{

    return (Cel * 1.8) + 32;
}

float FahToCel(float Fah)
{

    return (Fah - 32) * (5.0 / 9.0);
}


void HowToUseThisProg(void)
{
    printf("온도를 입력해주세요.(100 C c 212 F f )\n");
}
int main()
{
    float temp;
    char unit;

    HowToUseThisProg();

    scanf("%f %c", &temp, &unit);

    if (unit == 'C' || unit == 'c')
    {
        printf("%2.fºC는 %2.fºF 로 반환됩니다.\n", temp, CelToFah(temp));
    }
    else if (unit == 'F' || unit == 'f')
    {
        printf("%2.fºF는 %2.fºC 로 반환됩니다.\n", temp, FahToCel(temp));
    }
    else
    {
        printf("알 수 없는 단위입니다.다시 입력해주세요\n");
    }



    return 0;
}
*/

